db.createUser({
    user: "{{ mongodb_user_admin_name }}",
    pwd: "{{ mongodb_user_admin_password }}",
    roles: [{
            role: "userAdminAnyDatabase",
            db: "admin"
        }
    ]
});
